<Window x:Class="UDM18.Client.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="UDM18 â€” Cá» TÆ°á»›ng Trá»±c Tuyáº¿n" Width="1240" Height="820"
        MinWidth="980" MinHeight="680" WindowStartupLocation="CenterScreen"
        Background="{StaticResource Canvas}" FontFamily="Segoe UI" TextOptions.TextFormattingMode="Display">
    <Grid>
        <Grid.ColumnDefinitions><ColumnDefinition Width="210"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions>
        <Border Background="#102A43">
            <Grid Margin="20,26,20,20">
                <Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/><RowDefinition Height="*"/><RowDefinition Height="Auto"/></Grid.RowDefinitions>
                <StackPanel>
                    <Border Width="48" Height="48" CornerRadius="14" Background="#2563EB" HorizontalAlignment="Left">
                        <TextBlock Text="å¸¥" Foreground="White" FontSize="25" FontWeight="Bold" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Border>
                    <TextBlock Text="UDM18" Foreground="White" FontSize="25" FontWeight="Bold" Margin="0,12,0,0"/>
                    <TextBlock Text="XIANGQI CLIENT" Foreground="#9FB3C8" FontSize="11"/>
                </StackPanel>
                <StackPanel Grid.Row="1" Margin="0,34,0,0">
                    <TextBlock Text="ÄIá»€U HÆ¯á»NG" Foreground="#829AB1" FontSize="10" FontWeight="Bold" Margin="12,0,0,8"/>
                    <Button Style="{StaticResource NavButton}" Content="01   Káº¿t ná»‘i" Command="{Binding ShowConnectionCommand}"/>
                    <Button Style="{StaticResource NavButton}" Content="02   Sáº£nh Ä‘áº¥u" Command="{Binding ShowLobbyCommand}"/>
                    <Button Style="{StaticResource NavButton}" Content="03   PhĂ²ng Ä‘áº¥u" Command="{Binding ShowGameRoomCommand}"/>
                </StackPanel>
                <Border Grid.Row="3" Background="#173F5F" CornerRadius="11" Padding="12">
                    <StackPanel>
                        <TextBlock Text="PROTOCOL" Foreground="#9FB3C8" FontSize="10" FontWeight="Bold"/>
                        <TextBlock Text="v1.0 Â· TCP JSON" Foreground="White" FontSize="12" Margin="0,4,0,0"/>
                        <TextBlock Text="Server authoritative" Foreground="#9FB3C8" FontSize="11"/>
                    </StackPanel>
                </Border>
            </Grid>
        </Border>
        <Grid Grid.Column="1">
            <Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="*"/></Grid.RowDefinitions>
            <Border Background="#FFF7ED" BorderBrush="#FDBA74" BorderThickness="0,0,0,1" Padding="18,9">
                <Border.Style>
                    <Style TargetType="Border">
                        <Setter Property="Visibility" Value="Collapsed"/>
                        <Style.Triggers><DataTrigger Binding="{Binding IsDemoMode}" Value="True"><Setter Property="Visibility" Value="Visible"/></DataTrigger></Style.Triggers>
                    </Style>
                </Border.Style>
                <TextBlock Text="â—  CHáº¾ Äá»˜ DEMO Cá»¤C Bá»˜ â€” dá»¯ liá»‡u máº«u chá»‰ Ä‘á»ƒ xem giao diá»‡n, khĂ´ng dĂ¹ng lĂ m evidence nghiá»‡m thu." Foreground="#9A3412" FontWeight="SemiBold"/>
            </Border>
            <Grid Grid.Row="1" Margin="28,24,28,26">
                <ContentControl Content="{Binding CurrentPage}"/>
            </Grid>
        </Grid>
    </Grid>
</Window>
